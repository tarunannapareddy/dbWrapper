syntax = "proto3";

package dbservice;

message QueryRequest {
  string tag = 1;
  string query_request = 2;
}

service DBService {
  rpc execute(QueryRequest) returns (ExecuteResponse);
}

message ExecuteResponse {
  string response = 1;
}
